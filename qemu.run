#!/bin/bash
qemu-system-aarch64 -s \
    -machine virtualization=on \
    -machine secure=on \
    -machine virt \
    -cpu cortex-a72 \
    -smp cores=4 \
    -m 512 \
    -kernel ./out/ChicoryOS \
    -nographic \
    -serial mon:stdio
